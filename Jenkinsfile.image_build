#!/usr/bin/env groovy

// This Jenkinsfile uses the internal VITO shared library

@Library('lib')_

containerImageBuildPipeline {
  dockerfile           = 'Dockerfile'
  docker_registry_prod = 'vito-docker.artifactory.vgt.vito.be'
  image_name           = 'openeo-web-editor'
  image_tag            = '0.12.4'
  promotion_job        = false
  run_tests            = false
}
